spring:
  application:
    name: department
  data:
    mongodb:
      uri: mongodb://${MONGO_DATABASE_USER}:${MONGO_DATABASE_PASSWORD}@${MONGO_DATABASE_HOST}/${MONGO_DATABASE_NAME}
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

microservices:
  employee:
    url: http://${EMPLOYEE_SERVICE}:8080